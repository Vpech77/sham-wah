version: "3.9"

services:
  web-gui:
    build:
      context: ./web-gui
      dockerfile: Dockerfile.dev
    container_name: nuxt_dev
    ports:
      - "3000:3000"
    volumes:
      - ./web-gui:/app
      - /app/node_modules
    environment:
      NUXT_HOST: 0.0.0.0
      NUXT_PORT: 3000

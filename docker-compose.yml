version: "3.9"

services:
  gui:
    build:
      context: ./gui
      dockerfile: Dockerfile.dev
    container_name: nuxt_dev
    ports:
      - "3000:3000"
    volumes:
      - ./gui:/app
      - /app/node_modules
    environment:
      NUXT_HOST: 0.0.0.0
      NUXT_PORT: 3000
